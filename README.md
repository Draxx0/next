## Installation

```
# Install dependencies

pnpm install || npm install || yarn install

# Run container

make up || docker-compose up -d

# Run migrations

make generate || pnpx prisma generate

make migrate || pnpx prisma migrate dev

# Run app

pnpm run dev || npm run dev || yarn dev
```
